export const landingPaletteColors = {
  default: [
    "[--primary:theme(colors.landing.primary)]",
    "[--light:theme(colors.landing.light)]",
    "[--primaryDark:theme(colors.landing.primaryDark)]",
  ],
};
