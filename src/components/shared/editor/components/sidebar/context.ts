import { createContext } from "react";

import type imageEditorSidebarProps from "./type";

const ImageEditorSidebarContext = createContext({} as imageEditorSidebarProps);

export default ImageEditorSidebarContext;
